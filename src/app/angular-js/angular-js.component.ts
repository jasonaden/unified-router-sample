import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-angular-js',
  template: ''
})
export class AngularJSComponent {
  // Empty component, used only for rendering by the router
}
